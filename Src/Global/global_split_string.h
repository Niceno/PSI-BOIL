#include <string>
#include <vector>

#include "global_precision.h"

namespace boil {

const std::vector<std::string> split_string(std::string input, 
                                            const char * delim);
} /* boil */
